"""
Example: use the math_ops package
"""

from math_ops import add, subtract, multiply, divide

if __name__ == "__main__":
    a, b = 12, 4
    print("add:", add(a, b))
    print("subtract:", subtract(a, b))
    print("multiply:", multiply(a, b))
    print("divide:", divide(a, b))
